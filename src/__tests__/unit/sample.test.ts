describe("it needs to return a new deck with the same length", () => {
  test("Shuffles a deck", () => {});
});

describe("Check if it has shuffled anything by comparing strings", () => {
  test("Shuffle a deck twice", () => {});
});
